export interface IValidateCodeResponse {
  access_token: string;
  refresh_token: string;
  token_type: string;
}

export interface IValidateCode {
  accessToken: string;
  refreshToken: string;
  tokenType: string;
}
